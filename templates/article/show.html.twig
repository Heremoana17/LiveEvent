{% extends 'base.html.twig' %}

{% block title %}
	{{article.title}}
{% endblock %}

{% block body %}
	<section class="container">
		<h1>{{article.title}}</h1>
		<hr>
		<p>{{article.content|raw}}</p>
	</section>

{% endblock %}
